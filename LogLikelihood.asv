function log_p = LogLikelihood(K,y)
    N = size(y,1);
    %adding elements to diagonal of K to make it invertible
    K = K + 0.01*eye(size(K));
    det(K)
    log_p = -log(det(K)) - (y')*(inv(K))*(y) - (N/2)*log(2*pi);
end